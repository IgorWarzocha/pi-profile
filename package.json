{
  "name": "pi-profile",
  "private": true,
  "type": "module",
  "scripts": {
    "aggregate": "node src/server-aggregate.js",
    "deploy": "cd capsule && npx lakebed deploy",
    "publish": "npm run aggregate && npm run deploy",
    "test": "node --test"
  },
  "engines": { "node": ">=20" }
}
